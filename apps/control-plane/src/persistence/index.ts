// Couche Persistence — repositories Drizzle, migrations PostgreSQL (P-01+)
// Schémas des 17 tables dans packages/schema. Triggers INSERT-only sur tables d'audit (INV-04).
// Résolution du tenant via TenantMiddleware (INV-06).

export {};
